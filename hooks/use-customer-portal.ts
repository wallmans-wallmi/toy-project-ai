/**
 * Alias ל־`usePortalPhoneAuth` (אימות OTP בפורטל לקוח).
 * הלוגיקה נמצאת ב־`hooks/use-portal-phone-auth.ts`.
 */
export { usePortalPhoneAuth as useCustomerPortal } from "@/hooks/use-portal-phone-auth";
